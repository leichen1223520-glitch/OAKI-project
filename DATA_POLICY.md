# 数据保密与版本管理策略
# DATA_POLICY.md

**版本**：V1.0（2026-07-25）  
**适用范围**：OAKI项目全部数据资产  

---

## 一、数据分类与保密等级

| 等级 | 类别 | 内容示例 | 处置方式 |
|------|------|---------|---------|
| **L-S1（保密）** | 真实水厂原始数据 | 深圳46厂具体厂名、坐标、原始台账 | 本地存储，不上传GitHub |
| **L-S2（脱敏）** | 真实数据脱敏版 | 厂ID哈希、坐标模糊化、水量统计 | 经脱敏后可上传 |
| **L-P1（公开）** | 仿真数据 | BSM2G输出数据集 | 可公开发布（含数据集卡） |
| **L-P2（公开）** | 文献参数 | ASM文献先验范围 | 完整引用后可公开 |
| **L-P3（公开）** | 代码与配置 | 模型代码、训练脚本 | 可公开（不含API密钥） |

---

## 二、数据来源标注体系

所有数据在使用前必须标注来源层级：

```
[仿真]        — BSM2G机理仿真输出，非真实观测
[文献先验]    — 从文献获取的参数范围，非实验测定
[真实活动数据] — 真实水厂月度/过程数据（不含GHG直接测量）
[真实GHG]     — 实测N₂O/CH₄通量（阶段B，计划获取）
[小试]        — 实验室批次动力学实验（阶段B，计划开展）
[插补]        — 通过模型估算填补的数值
[掩蔽]        — 人工掩蔽的已知标签（用于训练实验）
[情景]        — 仿真情景分析结果
```

---

## 三、原始数据保护规则

### 3.1 不允许上传到GitHub的内容

- `data/raw/` 下的任何真实水厂原始文件
- 含有真实水厂名称、位置、许可证信息的文档
- 任何含个人信息的文件
- 模型配置中的访问密钥、API密钥、密码

### 3.2 .gitignore规则（见`.gitignore`）

```
data/raw/
data/interim/*_real*.csv
*.env
secrets/
```

### 3.3 脱敏方法

- 水厂ID：使用SHA-256哈希（保留前8位，如 `plant_a3f92b1c`）
- 地理位置：模糊至区级（不精确到厂址）
- 时间戳：保留年月，隐藏具体日期
- 运营联系人：完全删除

---

## 四、BSM2G数据版本管理

BSM2G仿真数据属于 `L-P1`（公开级），建议使用以下方案：

- **小数据集**（<100MB）：直接存入 `data/processed/bsm2g/`，提交到GitHub
- **大数据集**（≥100MB）：使用Git LFS或发布到Zenodo，在代码中仅保存URL和校验和

### 数据集校验和记录

每次生成/修改数据集时，在 `results/manifests/data_checksums.csv` 中记录：

```
文件名,生成时间,SHA256,大小(MB),BSM2G版本,随机种子,备注
bsm2g_standard_v1.parquet,2026-XX-XX,<hash>,XX,BSM2G vX.X,42,标准609天仿真
```

---

## 五、实验结果版本管理

### 5.1 命名规范

```
results/<类型>/<实验名>_<模型版本>_<数据版本>_<日期>_seed<随机种子>.<扩展名>

示例：
results/tables/baseline_attmmoe_v1_bsm2g-v1_20260801_seed42.csv
results/figures/ablation_ghg_rmse_oaki-pl-v1_bsm2g-v1_20260802_seed42.pdf
results/models/oaki_pl_v1_bsm2g-v1_fold1_seed42.pt
```

### 5.2 结果清单

所有已发布结果在 `results/manifests/results_manifest.csv` 中登记：

```
结果ID,类型,文件名,实验编号,模型版本,数据版本,随机种子,Git提交号,主要指标,备注
```

### 5.3 禁止行为

- 不得覆盖已被论文引用的结果文件
- 不得在未更新清单的情况下修改结果文件
- 不得在论文中引用未在清单中登记的数值

---

## 六、随机种子管理

所有实验必须固定随机种子，并在配置文件中记录：

```yaml
# configs/experiment_template.yaml
seed: 42                    # 主随机种子
numpy_seed: 42
torch_seed: 42
data_split_seed: 123        # 数据划分种子（独立）
mask_seed: 456              # 掩蔽实验种子（独立）
```

消融实验至少使用3个不同种子（42, 123, 456），报告均值±标准差。

---

## 七、数据共享规则

| 场景 | 规则 |
|------|------|
| 论文投稿附件 | 仅分享脱敏版或BSM2G仿真数据 |
| 公开代码仓库 | 提供生成脚本而非原始数据 |
| 向合作者共享 | 签署保密协议后可共享L-S1数据 |
| 向评审人共享 | 仅共享L-P1/L-P2层级数据 |

---

*更新历史：V1.0（2026-07-25）初始版本*
